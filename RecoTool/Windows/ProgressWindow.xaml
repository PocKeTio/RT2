<Window x:Class="RecoTool.Windows.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Progression" Height="200" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Window.Resources>
        <!-- Style pour la barre de progression -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#006A4D"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <Border x:Name="PART_Track" CornerRadius="4">
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        HorizontalAlignment="Left" 
                                        CornerRadius="4"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Conteneur principal avec effet d'ombre -->
    <Border CornerRadius="12" Background="White" 
            BorderBrush="#DDDDDD" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="3" Direction="315" Color="#44000000" Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- En-tête avec icône -->
                <RowDefinition Height="Auto"/>
                <!-- Message de statut -->
                <RowDefinition Height="20"/>
                <!-- Espacement -->
                <RowDefinition Height="Auto"/>
                <!-- Barre de progression -->
                <RowDefinition Height="Auto"/>
                <!-- Pourcentage -->
            </Grid.RowDefinitions>

            <!-- En-tête avec icône et titre -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                <!-- Icône animée -->
                <Border Background="#006A4D" CornerRadius="20" Width="40" Height="40" Margin="0,0,15,0">
                    <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" 
                               FontSize="18" Foreground="White" 
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="SpinTransform" CenterX="9" CenterY="9"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinTransform"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </Border>

                <!-- Titre -->
                <TextBlock Text="Processing in progress..." 
                           FontSize="18" FontWeight="SemiBold" 
                           Foreground="#333333" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message de statut -->
            <TextBlock x:Name="StatusMessage" Grid.Row="1" 
                       Text="Initializing..."
                       FontSize="14" Foreground="#666666" 
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>

            <!-- Barre de progression -->
            <ProgressBar x:Name="MainProgressBar" Grid.Row="3" 
                         Style="{StaticResource ModernProgressBar}"
                         Value="0" Maximum="100"
                         Margin="0,10,0,0"/>

            <!-- Pourcentage -->
            <TextBlock x:Name="PercentageText" Grid.Row="4" 
                       Text="0%"
                       FontSize="12" FontWeight="Medium"
                       Foreground="#006A4D" 
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>
        </Grid>
    </Border>
</Window>